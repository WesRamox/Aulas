<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
</head>
<body>
  <h1>Renderizando HTML Dinamico com EJS</h1>
  <p>Seja bem vindo <%= 'Wesley' %></p>
  <p>
    <%= message %>

    <% if (1===1) { %>
      <p>verdadeiro</p>
    <% } else { %>
      <p>falso</p>
    <% } %>

    <ul>
      <% for( let index = 0; index < 10; index++ ) { %>
        <li>4 * <%= index %> = <%= 4 * index %></li>
      <% } %>
    </ul>
  </p>
</body>
</html>